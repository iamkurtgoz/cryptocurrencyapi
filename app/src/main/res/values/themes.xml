<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AndroidApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">@font/quicksand_regular</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@color/colorBackground</item>
    </style>

    <style name="Theme.AndroidApp.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/colorBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.AndroidApp</item>
        <item name="android:includeFontPadding">false</item>
    </style>

</resources>